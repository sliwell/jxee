1. sscanf(const char * str, const char * format, ...)
	当输入数据格式中间匹配变量为空时，直接退出，后续匹配参数将无法进行匹配解析。
示例：
	字符串1：“123,2,23555"
	字符串2：“123,,23555"
	匹配格式：“%[^,],%*[^,],%s”,pstr1,pstr2
	在字符串1的情况下,pstr1="123",pstr2="23555"; 但在字符串2的情况下，pstr1=“123”，pstr2=NULL
	主要是因为模式"%*[^,]"匹配结果为空，虽然没有赋值变量，但仍然会造成函数退出解析。